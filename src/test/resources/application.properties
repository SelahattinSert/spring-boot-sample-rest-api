spring.application.name=camera-onboarding
spring.profiles.active=test
# when the JPA integration tests context is up, this config creates the h2 tables automatically
spring.jpa.hibernate.ddl-auto=update
#liquebase  --- cancel liquibase integration in the unit tests.
spring.liquibase.enabled=false
api.version=api/v1
